<project name="mobilizecarsweb" default="war" basedir="../">
	<property name="src" location="${basedir}/src"/>
	<property name="lib" location="${basedir}/lib"/>
	<property name="build" location="${basedir}/buildfiles"/>
	<property name="buildclasses" location="${build}/classes"/>
	<property name="mobilizecarscore" location="${basedir}/../mobilizecarscore/buildfiles/lib"/>
	<property name="apachetomcat" location="${basedir}/../../apache-tomcat-7.0.69/lib"/>	
	<property name="srcapp" location="${src}/com/mobilize/web/controller/application"/>
	<property name="srcser" location="${src}/com/mobilize/web/controller/service"/>
	<property name="comlib" location="${basedir}/WebContent/WEB-INF/lib"/>
	<property name="srcmobcore" location="${basedir}/../mobilizecarscore/src"/>

	<path id='compile.classpath'>
		<fileset dir="${mobilizecarscore}">
			<include name='*.jar'/>
		</fileset>
		<fileset dir="${apachetomcat}">
			<include name='*.jar'/>
		</fileset>
		<fileset dir="${lib}">
			<include name='*.jar'/>
		</fileset>
		<fileset dir="${comlib}">
			<include name='*.jar'/>
		</fileset>
	</path>	
	
	<target name="init">
		<mkdir dir="${build}"/>
		<mkdir dir="${buildclasses}"/>		
	</target>
	
	<target name="compile" depends="init">
		<javac srcdir= "${src}" destdir="${buildclasses}">
		<classpath refid="compile.classpath"/>
		</javac>
	</target>
	
	
	<target name ="war" depends ="compile">
		<war destfile="dist/mobilizecarsweb.war" webxml="${basedir}/WebContent/WEB-INF">
		<fileset dir="${basedir}/WebContent"/>
		<lib dir="${basedir}/WebContent/WEB-INF/lib"/>
		<lib dir="${mobilizecarscore}"/>
		<classes dir ="${buildclasses}"/>
		</war>		

	</target>
	
</project>